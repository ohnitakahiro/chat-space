.ChatRoom__Info{data: {message: {id: message.id}}}
  .ChatRoom__Info__UserName
    = message.user.name
  .ChatRoom__Info__Timestamp
    = message.created_at.strftime("%Y年%m月%d日 %H時%M分")
- if message.body.present?
  %p.ChatRoom__Message
    = message.body
= image_tag message.image.url, class: 'ChatRoom__Message__Image' if message.image.present?
